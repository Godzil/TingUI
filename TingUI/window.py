from .widget import Widget

class Window:
    """A window define a collection of widgets
    and is used to display these widget on screen.
    There currently be only one window displayed at a time,
    if you open a new one it will overlay the currently
    displayed, and will take the exclusive access to inputs.
    """
    pass